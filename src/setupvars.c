
#include "setupvars.h"

int setup(void) {

    gfx_Begin();
    // excludes a row for the selection/options bar, will expand later when scrolling and bigger map support is added.
    int mapdata[20 * 14];
    int curX = 0, curY = 0, select = 0;

}